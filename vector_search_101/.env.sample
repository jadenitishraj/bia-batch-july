# OpenAI — optional for one comparison section only
# The main practical runs locally with sentence-transformers and FAISS.
OPENAI_API_KEY=sk-your-key-here
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# No LLM is required for this practical.
# Keep keys in .env only. Never hardcode API keys in notebooks or source files.
